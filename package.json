{
  "name": "ouroboro",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "bench": "npm run build && node scripts/bench.mjs",
    "smoke": "npm run build && node scripts/smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  },
  "description": "Jogo da cobrinha em que o corpo e a moeda de energia"
}
